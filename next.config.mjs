/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['fakestoreapi.com'], // Allow loading images from fakestoreapi.com
      },
    };

export default nextConfig;
